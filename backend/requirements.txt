# Phases 1-5 have ZERO runtime dependencies (standard library only).
# Dev/test tooling:
pytest>=8.0

# Phase 6+ (model training stack):
torch>=2.2
numpy>=1.26

# Phase 10 (document ingestion):
pypdf>=6.0
python-docx>=1.1

# Phase 12 (knowledge graph) — also pulled in by torch:
networkx>=3.2

# Phase 13 (embeddings + vector store):
psycopg[binary]>=3.1
pgvector>=0.2

# Phase 14 (RAG generation via Groq — Llama 3.3 70B):
groq>=0.11

# Loads backend/.env (GROQ_API_KEY, DATABASE_URL, …) automatically:
python-dotenv>=1.0

# Declared here for later phases (kept commented until their phase lands):
# fastapi>=0.110
# uvicorn>=0.29
# pydantic>=2.6
# pandas>=2.2
# redis>=5.0
