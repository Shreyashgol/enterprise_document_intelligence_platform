# Phases 1-5 have ZERO runtime dependencies (standard library only).
# Dev/test tooling:
pytest>=8.0

# Phase 6+ (model training stack):
torch>=2.2
numpy>=1.26

# Phase 7C/7D (BERT / BERT-CRF) need `transformers` — installed separately via
# requirements-transformers.txt. It is intentionally NOT a hard dependency here:
# the deployed service trains/serves the from-scratch BiLSTM tagger (the encoder
# imports are lazy), so the free-tier build stays lean. Install the extra only to
# train or serve the transformer models.

# Phase 10 (document ingestion):
pypdf>=6.0
python-docx>=1.1

# Phase 12 (knowledge graph) — also pulled in by torch:
networkx>=3.2

# Phase 13 (embeddings + vector store):
psycopg[binary]>=3.1
pgvector>=0.2

# Phase 14 (RAG generation via Groq — Llama 3.3 70B):
groq>=0.11

# Loads backend/.env (GROQ_API_KEY, DATABASE_URL, …) automatically:
python-dotenv>=1.0

# API layer (FastAPI service):
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.6
